framework:
    rate_limiter:
        public_booking:
            policy: 'sliding_window'
            limit: 5
            interval: '1 minute'
            lock_factory: 'lock.factory'
        public_suivi:
            policy: 'sliding_window'
            limit: 60
            interval: '1 minute'
            lock_factory: 'lock.factory'
        companion_upload:
            policy: 'sliding_window'
            limit: 20
            interval: '1 minute'
            lock_factory: 'lock.factory'
