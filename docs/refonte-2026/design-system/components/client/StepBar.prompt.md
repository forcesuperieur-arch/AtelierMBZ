One-line: the four bars on the black customer header, with the step also written in words.

```jsx
<StepBar total={4} current={2} label="Étape 2 sur 4 · ce dont vous avez besoin" />
```

- Designed for the black header only — the inactive bar colour (#4a4a4a) assumes it.
- Never show the bars without the sentence: the customer books twice a year and cannot learn the flow.
