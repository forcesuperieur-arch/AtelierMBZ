<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: DejaVu Sans, sans-serif; font-size: 11px; color: #333; margin: 20px; }
        .header { border-bottom: 2px solid #2563eb; padding-bottom: 10px; margin-bottom: 20px; }
        .header-table { width: 100%; border-collapse: collapse; }
        .header-left, .header-right { vertical-align: top; }
        .header-right { text-align: right; }
        .header h1 { color: #2563eb; margin: 0; font-size: 18px; }
        .logo { max-width: 120px; max-height: 60px; margin-bottom: 8px; }
        .atelier-meta { font-size: 10px; color: #555; line-height: 1.4; }
        .info-grid { display: table; width: 100%; margin-bottom: 20px; }
        .info-col { display: table-cell; width: 50%; vertical-align: top; }
        .info-col-3 { display: table-cell; width: 33%; vertical-align: top; }
        .label { font-weight: bold; color: #666; }
        .section { margin-bottom: 24px; page-break-inside: avoid; }
        .section-title { font-size: 13px; font-weight: bold; color: #2563eb; border-bottom: 1px solid #2563eb; padding-bottom: 4px; margin-bottom: 10px; }
        .recap-box { background: #f8fafc; border: 1px solid #cbd5e1; padding: 10px 14px; margin: 8px 0; }
        .notes-box { background: #eff6ff; border-left: 4px solid #3b82f6; padding: 8px 12px; margin: 8px 0; }
        .photos-table { width: 100%; border-collapse: collapse; margin-top: 10px; }
        .photos-table td { width: 33%; padding: 6px; vertical-align: top; }
        .photo-card { border: 1px solid #ddd; background: #fafafa; padding: 6px; }
        .photo-card img { width: 100%; max-height: 120px; object-fit: cover; }
        .photo-caption { font-size: 9px; color: #555; margin-top: 4px; }
        .signature-grid { display: table; width: 100%; margin-top: 20px; }
        .signature-col { display: table-cell; width: 50%; vertical-align: top; padding-right: 12px; }
        .signature-box { border: 1px solid #ccc; padding: 10px; min-height: 80px; background: #fafafa; }
        .signature-box img { max-width: 180px; max-height: 60px; }
        .signature-label { font-size: 10px; color: #666; margin-bottom: 6px; }
        .meta-row { font-size: 10px; color: #666; margin-top: 4px; }
    </style>
</head>
<body>
    <div class="header">
        <table class="header-table">
            <tr>
                <td class="header-left">
                    {% if logo_data_uri %}
                    <img src="{{ logo_data_uri }}" alt="Logo atelier" class="logo">
                    {% endif %}
                    <div class="atelier-meta">
                        <strong>{{ atelier and atelier.nom ? atelier.nom : 'Atelier Moto' }}</strong><br>
                        {% if atelier and atelier.adresse %}{{ atelier.adresse }}<br>{% endif %}
                        {% if atelier and (atelier.cp or atelier.ville) %}{{ atelier.cp }} {{ atelier.ville }}<br>{% endif %}
                        {% if atelier and atelier.telephone %}Tél : {{ atelier.telephone }}<br>{% endif %}
                        {% if atelier and atelier.email %}{{ atelier.email }}<br>{% endif %}
                        {% if atelier and atelier.siret %}SIRET : {{ atelier.siret }}{% endif %}
                    </div>
                </td>
                <td class="header-right">
                    <h1>ÉTAT DES LIEUX D'ENTRÉE</h1>
                    <p><strong>RDV n° {{ rdv.id }}</strong> — {{ rdv.dateRdv|date('d/m/Y') }} à {{ rdv.heureRdv|date('H:i') }}</p>
                    <p class="meta-row">Document établi le {{ edl.createdAt|date('d/m/Y H:i') }}</p>
                </td>
            </tr>
        </table>
    </div>

    <div class="info-grid">
        <div class="info-col">
            <p class="label">Client</p>
            <p>{{ edl.snapClientPrenom ?? (rdv.client ? rdv.client.prenom : '') }} {{ edl.snapClientNom ?? (rdv.client ? rdv.client.nom : '') }}</p>
        </div>
        <div class="info-col">
            <p class="label">Véhicule</p>
            {% set vMarque = edl.snapVehiculeMarque ?? (rdv.vehicule ? rdv.vehicule.marque : '') %}
            {% set vModele = edl.snapVehiculeModele ?? (rdv.vehicule ? rdv.vehicule.modele : '') %}
            {% set vPlaque = edl.snapVehiculePlaque ?? (rdv.vehicule ? rdv.vehicule.plaque : '') %}
            <p>{{ vMarque }} {{ vModele }}<br>
               Plaque : {{ vPlaque }}</p>
        </div>
    </div>

    <div class="section">
        <div class="section-title">Constat à l'entrée du véhicule</div>
        <div class="recap-box">
            <table style="width:100%;border-collapse:collapse;">
                <tr>
                    <td style="width:50%;border-right:1px solid #cbd5e1;padding-right:12px;">
                        <span class="label">Kilométrage constaté</span><br>
                        <span style="font-size:16px;font-weight:bold;color:#2563eb;">{{ edl.kilometrage is not null ? edl.kilometrage ~ ' km' : 'Non renseigné' }}</span>
                    </td>
                    <td style="width:50%;padding-left:12px;">
                        <span class="label">Niveau de carburant</span><br>
                        <span style="font-size:16px;font-weight:bold;color:#2563eb;">{{ niveau_carburant_label }}</span>
                    </td>
                </tr>
            </table>
        </div>
    </div>

    {% if edl.observations %}
    <div class="section">
        <div class="section-title">Observations</div>
        <div class="notes-box">
            <p>{{ edl.observations|nl2br }}</p>
        </div>
    </div>
    {% endif %}

    {% if photos_entree is defined and photos_entree|length > 0 %}
    <div class="section">
        <div class="section-title">Photos d'entrée du véhicule</div>
        <table class="photos-table">
            <tr>
                {% for photo in photos_entree %}
                    <td>
                        <div class="photo-card">
                            <img src="{{ photo.src }}" alt="{{ photo.label|default("Photo d'entrée") }}">
                            <div class="photo-caption">
                                {{ photo.label|default("Photo d'entrée") }}{% if photo.takenAt %}<br>{{ photo.takenAt }}{% endif %}
                            </div>
                        </div>
                    </td>
                    {% if loop.index is divisible by(3) and not loop.last %}</tr><tr>{% endif %}
                {% endfor %}
            </tr>
        </table>
    </div>
    {% endif %}

    <div class="section">
        <div class="signature-grid">
            <div class="signature-col">
                <p class="signature-label"><strong>Signature client</strong></p>
                <div class="signature-box">
                    {% if edl.signatureClient %}
                    <img src="{{ edl.signatureClient }}">
                    {% else %}
                    <p style="color:#999;font-size:10px;">Non signé</p>
                    {% endif %}
                </div>
                {% if edl.signedAt %}
                <p class="meta-row">Signé le {{ edl.signedAt|date('d/m/Y H:i') }}</p>
                {% endif %}
            </div>
            <div class="signature-col">
                <p class="signature-label"><strong>Établi par l'atelier</strong></p>
                <div class="signature-box">
                    {% if edl.signedByNom %}
                    <p>{{ edl.signedByNom }}</p>
                    {% else %}
                    <p style="color:#999;font-size:10px;">Non renseigné</p>
                    {% endif %}
                </div>
            </div>
        </div>
        <p style="font-size: 9px; margin-top: 8px;">Le client reconnaît l'exactitude de l'état des lieux d'entrée de son véhicule décrit ci-dessus.</p>
    </div>

    {% if edl.signedHash %}
    <div class="section" style="margin-top: 40px;">
        <p style="font-size: 8px; color: #999; word-break: break-all;">
            Empreinte d'intégrité : {{ edl.signedHash }}<br>
            {% if edl.signedAt %}Horodatage de signature : {{ edl.signedAt|date('d/m/Y H:i:s') }}{% endif %}
        </p>
    </div>
    {% endif %}
</body>
</html>
