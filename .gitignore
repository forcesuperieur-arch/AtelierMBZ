# Python
__pycache__/
*.py[cod]
.pytest_cache/

# Local app secrets / runtime artefacts
ateliermoto-main/backend/.secret_key
*.log

# Local database dumps stay out of Git.
# The schema/versioning source of truth is `ateliermoto-main/backend/alembic/versions/`
# and the bootstrap data lives in the seed scripts under `ateliermoto-main/backend/`.
backups/*.sql
